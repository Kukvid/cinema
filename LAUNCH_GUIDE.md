# 🎬 Руководство по запуску Cinema Management System

## 📋 Содержание
1. [🆕 Последние изменения](#последние-изменения)
2. [Обзор проекта](#обзор-проекта)
3. [Предварительные требования](#предварительные-требования)
4. [Установка PostgreSQL](#установка-postgresql)
5. [Настройка Backend](#настройка-backend)
6. [Настройка Frontend](#настройка-frontend)
7. [Запуск приложения](#запуск-приложения)
8. [Тестовые данные](#тестовые-данные)
9. [Функциональность](#функциональность)
10. [Решение проблем](#решение-проблем)

---

## 🆕 Последние изменения

### ✨ Система жанров (27.11.2024)

**Backend изменения:**
- ✅ Добавлена модель `Genre` с 12 популярными жанрами
- ✅ Создана many-to-many связь между Film и Genre
- ✅ Новый API endpoint: `/api/v1/genres` (CRUD операции)
- ✅ Обновлен endpoint `/api/v1/films`:
  - При создании/обновлении: отправляйте `genre_ids: [1, 2, 3]`
  - При получении: возвращается `genres: [{id, name}, ...]`
- ✅ Миграция БД (003) - автоматически переносит старые данные
- ✅ Seed обновлен - создает 12 жанров и связывает с фильмами

**Frontend требует обновления:**
📖 **Инструкции:** См. файл `FRONTEND_MIGRATION_GUIDE.md`

Основные изменения:
- Заменить `film.genre` (строка) на `film.genres` (массив объектов)
- Использовать мультиселект при создании/редактировании фильмов
- Обновить отображение жанров (Chip вместо текста)
- Добавить фильтрацию по ID жанра вместо текста

**Как применить изменения:**
```bash
# Backend уже обновлен и запущен!
# Проверьте: http://localhost:8000/docs

# Frontend - следуйте инструкциям в FRONTEND_MIGRATION_GUIDE.md
```

---

## 🎯 Обзор проекта

**Cinema Management System** - полнофункциональная система управления сетью кинотеатров с:

### Backend (FastAPI + PostgreSQL)
- ✅ 20 моделей БД (SQLAlchemy 2.0)
- ✅ 55+ API endpoints
- ✅ **NEW:** Система жанров с many-to-many связями
- ✅ JWT аутентификация
- ✅ Система бронирования билетов
- ✅ Бонусная программа (10% кэшбэк)
- ✅ Промокоды и скидки
- ✅ Кинобар с предзаказами
- ✅ QR-коды для билетов
- ✅ Договоры проката с дистрибьюторами
- ✅ ~800-1100 тестовых записей

### Frontend (React + Material-UI)
- ✅ 11 страниц (публичные + личный кабинет + админ)
- ✅ 6 переиспользуемых компонентов
- ✅ Красивый темный UI (Netflix-стиль)
- ✅ Интерактивная схема зала
- ✅ Бронирование с кинобаром
- ✅ Применение промокодов и бонусов
- ✅ Админ-панель для управления

---

## 💻 Предварительные требования

### Обязательное ПО:
- **Python 3.10+** - [Скачать](https://www.python.org/downloads/)
- **Node.js 16+** и npm - [Скачать](https://nodejs.org/)
- **PostgreSQL 14+** - [Скачать](https://www.postgresql.org/download/)
- **Git** (опционально) - [Скачать](https://git-scm.com/)

### Проверка установки:
```bash
python --version      # Python 3.10.7 или выше
node --version        # v16.x.x или выше
npm --version         # 8.x.x или выше
psql --version        # PostgreSQL 14.x или выше
```

---

## 🐘 Установка PostgreSQL

### Windows:
1. Скачать установщик: https://www.postgresql.org/download/windows/
2. Запустить установщик, выбрать:
   - Порт: `5432`
   - Пароль для пользователя `postgres`: запомнить!
   - Locale: `Russian, Russia` или `Default locale`
3. После установки запустить pgAdmin 4 или использовать командную строку

### Создание базы данных:
```bash
# Войти в psql (потребуется пароль postgres)
`psql -U postgres`

# В psql выполнить:
CREATE DATABASE cinema_db;
CREATE USER cinema_user WITH PASSWORD 'cinema_password123';
GRANT ALL PRIVILEGES ON DATABASE cinema_db TO cinema_user;
\q
```

## ⚙️ Настройка Backend

### Шаг 1: Перейти в папку backend
```bash
cd backend
```

### Шаг 2: Создать виртуальное окружение (если ещё не создано)
```bash
# Windows
python -m venv .venv
..\.venv\Scripts\activat
```

### Шаг 3: Установить зависимости
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**Ожидаемые пакеты:** FastAPI, SQLAlchemy, psycopg2-binary, Alembic, pydantic, python-jose, passlib, qrcode, и др. (~21 пакет)

### Шаг 4: Настроить .env файл
```bash
# Скопировать пример
cp .env.example .env

# Открыть .env в любом редакторе и настроить:
```

**Содержимое .env:**
```ini
# Database
DATABASE_URL=postgresql+asyncpg://postgres:ВАШ_ПАРОЛЬ@localhost:5432/cinema_db

# Security
SECRET_KEY=ваш_секретный_ключ_минимум_32_символа_случайных
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# App
APP_NAME=Cinema Management System
DEBUG=True

# CORS
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

**Генерация SECRET_KEY:**
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### Шаг 5: Создать схему базы данных
```bash
python create_db.py
```

**Вывод:**
```
🔍 Проверка подключения к PostgreSQL...
✅ Подключение успешно!

⚠️  ВНИМАНИЕ: Эта операция удалит все существующие таблицы!

Database URL: postgresql+asyncpg://postgres:***@localhost:5432/cinema_db

Продолжить? (yes/no): yes

🗑️  Удаление старых таблиц...
✅ Старые таблицы удалены

🔨 Создание новых таблиц...
✅ Таблицы созданы успешно!

📊 Созданные таблицы:
   1. roles
   2. cinemas
   3. halls
   ...
   19. reports

✅ База данных готова к использованию!
```

### Шаг 6: Заполнить тестовыми данными
```bash
python seed.py
```

**Вывод (процесс займет 5-10 секунд):**
```
🌱 Начало заполнения базы данных тестовыми данными...

✅ Создано 3 ролей
✅ Создано 3 кинотеатров
✅ Создано 6 залов
✅ Создано 576 мест
✅ Создано 20 фильмов
✅ Создано 3 дистрибьютора
✅ Создано 20 договоров проката
✅ Создано 8 пользователей
✅ Создано 8 бонусных счетов
✅ Создано 52 сеансов
✅ Создано 7 промокодов
✅ Создано 57 товаров кинобара
✅ Создано 10 заказов с билетами

🎉 База данных успешно заполнена!

📊 Статистика:
   - Всего записей: ~900
   - Пользователей: 8
   - Фильмов: 20
   - Сеансов: 52
   - Билетов: 22
```

### Шаг 7: Запустить Backend сервер
```bash
uvicorn app.main:app --reload
```

**Вывод:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [12345] using WatchFiles
INFO:     Started server process [12346]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### Проверка Backend:
- **API документация (Swagger):** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
- **Health check:** http://localhost:8000/ (должно вернуть приветствие)

**Не закрывайте этот терминал!** Сервер должен продолжать работать.

---

## 🎨 Настройка Frontend

### Шаг 1: Открыть НОВЫЙ терминал и перейти в frontend
```bash
cd frontend
```

### Шаг 2: Установить зависимости
```bash
npm install
```

**Ожидаемые пакеты:** React, React Router, Material-UI, Axios, React Hook Form и др. (~30+ пакетов)

**Время установки:** 1-3 минуты в зависимости от скорости интернета.

### Шаг 3: Настроить .env файл
```bash
# Скопировать пример
cp .env.example .env

# Содержимое .env (уже должно быть правильным):
REACT_APP_API_BASE_URL=http://localhost:8000/api/v1
```

### Шаг 4: Запустить Frontend
```bash
npm start
```

**Вывод:**
```
Compiled successfully!

You can now view cinema-frontend in the browser.

  Local:            http://localhost:3000
  On Your Network:  http://192.168.x.x:3000

Note that the development build is not optimized.
To create a production build, use npm run build.

webpack compiled successfully
```

**Браузер должен автоматически открыться** на http://localhost:3000

---

## 🚀 Запуск приложения

### Полная последовательность (краткая версия):

**Терминал 1 (Backend):**
```bash
cd backend
source .venv/Scripts/activate    # Windows Git Bash
pip install -r requirements.txt  # Если ещё не сделано
python create_db.py              # Если ещё не сделано
python seed.py                   # Если ещё не сделано
uvicorn app.main:app --reload
```

**Терминал 2 (Frontend):**
```bash
cd frontend
npm install                      # Если ещё не сделано
npm start
```

### Проверка работы:
1. Backend работает: http://localhost:8000/docs
2. Frontend открыт: http://localhost:3000
3. Видна главная страница с каталогом фильмов

---

## 🔑 Тестовые данные

### Учетные записи для входа:

#### Администратор (полный доступ):
- **Email:** `admin@cinema.ru`
- **Password:** `admin123`
- **Доступ:** Все функции + админ-панель

#### Менеджер:
- **Email:** `manager@cinema.ru`
- **Password:** `manager123`
- **Доступ:** Управление сеансами и заказами

#### Обычные пользователи (все с паролем `user123`):
- `user@example.com` - Пользователь Тестовый
- `ivan@example.com` - Иван Иванов
- `maria@example.com` - Мария Петрова
- `alexey@example.com` - Алексей Сидоров
- `elena@example.com` - Елена Смирнова
- `dmitry@example.com` - Дмитрий Козлов

**Все пользователи имеют 100-500 бонусов на счету!**

### Активные промокоды:

| Код | Скидка | Мин. сумма | Описание |
|-----|--------|-----------|----------|
| `WELCOME10` | 10% | 500₽ | Для новых пользователей |
| `NEWYEAR2024` | 500₽ | 1000₽ | Новогодняя акция |
| `BIRTHDAY20` | 20% | 300₽ | В день рождения |
| `WEEKEND15` | 15% | 600₽ | На выходных |
| `STUDENT` | 200₽ | 400₽ | Студенческая скидка |

### Кинотеатры:
- **КиноПарк "Октябрь"** - Москва, ул. Тверская, д. 10
- **КиноМакс "Европа"** - Санкт-Петербург, Невский пр., д. 100
- **Синема Сити** - Казань, ул. Баумана, д. 58

### Фильмы в прокате (20 штук):
- Оппенгеймер (8.5 ⭐, 180 мин)
- Барби (7.0 ⭐, 114 мин)
- Дюна: Часть вторая (8.7 ⭐, 166 мин)
- Побег из Шоушенка (9.3 ⭐)
- Темный рыцарь (9.0 ⭐)
- Интерстеллар (8.6 ⭐)
- И другие...

---

## ✨ Функциональность

### Для всех пользователей (не авторизованных):
1. ✅ Просмотр каталога фильмов
2. ✅ Фильтрация по жанрам
3. ✅ Поиск по названию
4. ✅ Просмотр детальной информации о фильме
5. ✅ Просмотр расписания сеансов
6. ✅ Регистрация

### Для авторизованных пользователей:
7. ✅ Бронирование билетов
8. ✅ Выбор мест на интерактивной схеме зала
9. ✅ Добавление товаров из кинобара
10. ✅ Применение промокодов
11. ✅ Использование бонусных баллов
12. ✅ Mock оплата
13. ✅ Просмотр QR-кодов билетов
14. ✅ Просмотр истории заказов
15. ✅ Просмотр бонусного баланса
16. ✅ Редактирование профиля

### Для администраторов:
17. ✅ Управление кинотеатрами (CRUD)
18. ✅ Управление залами и местами
19. ✅ Управление фильмами (+ загрузка постеров)
20. ✅ Управление сеансами
21. ✅ Управление дистрибьюторами
22. ✅ Управление договорами проката
23. ✅ Управление товарами кинобара
24. ✅ Управление промокодами
25. ✅ Просмотр статистики

---

## 🧪 Тестовый сценарий

### Сценарий 1: Регистрация и первая покупка
1. Откройте http://localhost:3000
2. Нажмите "Войти" → "Регистрация"
3. Заполните форму (любые данные)
4. После регистрации вы автоматически войдете
5. Выберите фильм → "Купить билет"
6. Выберите сеанс
7. Выберите места на схеме зала (кликните на зеленые места)
8. Добавьте товары из кинобара (+ кнопка)
9. Введите промокод: `WELCOME10`
10. Нажмите "Применить промокод"
11. Нажмите "Оплатить"
12. Видна страница успешной оплаты с QR-кодами

### Сценарий 2: Вход под администратором
1. Выйдите из аккаунта (иконка пользователя → Выход)
2. Войдите: `admin@cinema.ru` / `admin123`
3. В меню появится "Админ-панель"
4. Перейдите в "Админ-панель"
5. Попробуйте:
   - Создать новый кинотеатр
   - Добавить фильм
   - Создать сеанс
   - Посмотреть статистику

### Сценарий 3: Использование бонусов
1. Войдите под пользователем с бонусами
2. Перейдите в "Профиль" → видно количество бонусов
3. Создайте заказ
4. Поставьте галочку "Использовать бонусы"
5. Введите количество (например, 50)
6. Сумма уменьшится
7. После оплаты начислятся новые бонусы (10% от суммы)

---

## 🛠️ Решение проблем

### Проблема: Backend не запускается

**Ошибка:** `ModuleNotFoundError: No module named 'fastapi'`
**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source .venv/Scripts/activate  # или .venv\Scripts\activate

# Переустановите зависимости
pip install -r requirements.txt
```

**Ошибка:** `psycopg2.OperationalError: could not connect to server`
**Решение:**
- Проверьте, что PostgreSQL запущен
- Windows: Откройте "Службы" → найдите "postgresql-x64-14" → Запустите
- Проверьте DATABASE_URL в .env (правильный пароль, порт 5432)

**Ошибка:** `sqlalchemy.exc.ProgrammingError: (psycopg2.errors.UndefinedTable) relation "users" does not exist`
**Решение:**
```bash
# Пересоздайте БД
python create_db.py
python seed.py
```

### Проблема: Frontend не запускается

**Ошибка:** `npm ERR! code ENOENT`
**Решение:**
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

**Ошибка:** `Module not found: Error: Can't resolve '@mui/material'`
**Решение:**
```bash
npm install @mui/material @emotion/react @emotion/styled
```

### Проблема: Frontend не подключается к Backend

**Ошибка в консоли браузера:** `Network Error` или `ERR_CONNECTION_REFUSED`
**Решение:**
- Убедитесь, что Backend запущен (http://localhost:8000/docs открывается)
- Проверьте REACT_APP_API_BASE_URL в frontend/.env
- Проверьте CORS_ORIGINS в backend/.env

### Проблема: "403 Forbidden" при создании данных

**Решение:**
- Войдите под администратором: `admin@cinema.ru` / `admin123`
- Некоторые действия требуют роль admin

### Проблема: Пустой каталог фильмов

**Решение:**
```bash
# В backend директории
python seed.py
```

---

## 📊 Структура проекта

```
Cinema/
├── backend/                      # FastAPI Backend
│   ├── app/
│   │   ├── models/              # SQLAlchemy модели (19 файлов)
│   │   ├── schemas/             # Pydantic схемы (12 файлов)
│   │   ├── routers/             # API endpoints (9 файлов)
│   │   ├── services/            # Бизнес-логика
│   │   ├── utils/               # Утилиты (security, qr_generator)
│   │   ├── config.py            # Настройки
│   │   ├── database.py          # DB setup
│   │   └── main.py              # FastAPI app
│   ├── alembic/                 # Миграции БД
│   ├── create_db.py             # Создание схемы БД
│   ├── seed.py                  # Тестовые данные
│   ├── requirements.txt         # Python зависимости
│   ├── .env                     # Конфигурация (НЕ в Git!)
│   └── README_SETUP.md          # Документация
│
├── frontend/                    # React Frontend
│   ├── src/
│   │   ├── pages/              # 11 страниц
│   │   │   ├── Home.js
│   │   │   ├── FilmDetail.js
│   │   │   ├── SessionBooking.js
│   │   │   ├── Login.js
│   │   │   ├── Register.js
│   │   │   ├── Profile.js
│   │   │   ├── MyTickets.js
│   │   │   └── admin/          # 4 админ-страницы
│   │   ├── components/         # 6 компонентов
│   │   ├── context/            # AuthContext
│   │   ├── api/                # API клиенты
│   │   ├── App.js
│   │   └── index.js
│   ├── public/
│   ├── package.json
│   └── .env
│
├── agents/                      # Инструкции для агентов
│   ├── backend-agent.md
│   ├── database-agent.md
│   ├── frontend-agent.md
│   └── tester-agent.md
│
├── CLAUDE.md                    # Документация для Claude Code
├── LAUNCH_GUIDE.md              # Этот файл
└── README.md                    # Основная документация
```

---

## 🎯 Следующие шаги

### Для разработки:
1. ✅ Приложение запущено и работает
2. 📝 Изучите код в `backend/app/` и `frontend/src/`
3. 🧪 Протестируйте все функции
4. 🎨 Настройте UI под свои предпочтения
5. 🔧 Добавьте новые функции

### Для production:
1. Создайте новый SECRET_KEY
2. Настройте production БД
3. Отключите DEBUG=False
4. Настройте HTTPS
5. Настройте реальные платежи
6. Добавьте email уведомления
7. Настройте backup БД

---

## 📞 Дополнительная помощь

### Документация:
- **Backend:** `backend/README_SETUP.md`
- **Quickstart:** `backend/QUICKSTART.md`
- **Модели БД:** `backend/MODELS_RELATIONSHIPS.md`
- **API:** http://localhost:8000/docs

### Полезные команды:

**Backend:**
```bash
# Проверить миграции Alembic
cd backend
alembic current

# Создать новую миграцию
alembic revision --autogenerate -m "описание"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1

# Очистить БД и пересоздать
python create_db.py
python seed.py
```

**Frontend:**
```bash
# Проверить ошибки
npm run build

# Очистить кэш
rm -rf node_modules package-lock.json
npm install
```

**PostgreSQL:**
```bash
# Войти в БД
psql -U postgres -d cinema_db

# Посмотреть таблицы
\dt

# Посмотреть данные
SELECT * FROM users;

# Выйти
\q
```

---

## ✅ Чек-лист запуска

- [ ] PostgreSQL установлен и запущен
- [ ] База данных `cinema_db` создана
- [ ] Python 3.10+ установлен
- [ ] Node.js 16+ установлен
- [ ] Backend зависимости установлены (`pip install -r requirements.txt`)
- [ ] Frontend зависимости установлены (`npm install`)
- [ ] Backend .env настроен (DATABASE_URL, SECRET_KEY)
- [ ] Frontend .env настроен (REACT_APP_API_BASE_URL)
- [ ] Схема БД создана (`python create_db.py`)
- [ ] Тестовые данные загружены (`python seed.py`)
- [ ] Backend запущен (`uvicorn app.main:app --reload`)
- [ ] Frontend запущен (`npm start`)
- [ ] http://localhost:8000/docs открывается
- [ ] http://localhost:3000 показывает каталог фильмов
- [ ] Успешный вход под `admin@cinema.ru` / `admin123`

---

## 🎉 Готово!

Приложение полностью настроено и готово к использованию!

**Backend API:** http://localhost:8000/docs
**Frontend UI:** http://localhost:3000

Приятного использования! 🍿🎬
